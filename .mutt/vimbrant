#
# title:    vimbrant - a mutt color scheme
# author:   thayer williams
# website:  http://cinderwick.ca/
#

color   normal      color244        default     # default colours
color   index       color197        default ~N  # new messages
color   index       color098        default ~F  # flagged messages
color   index       color027        default ~T  # tagged messages
color   index       color238        default ~D  # deleted messages
color   body        color161        default     (https?|ftp)://[\-\.,/%~_:?&=\#a-zA-Z0-9]+
color   body        color161        default     [\-\.+_a-zA-Z0-9]+@[\-\.a-zA-Z0-9]+
color   attachment  color135        default     # attachments
color   signature   brightwhite     default     # sigs
color   search      color196        default     # highlight results

color   indicator   color255        default     # currently highlighted message
color   error       color124        default     # error messages
color   status      brightwhite     color235    # status line (unread msgs, current dir, etc.)
color   markers     brightwhite     color235    # shortcut statusline (q:Quit, etc.)
color   tree        color161        default     # tree thread arrows
color   tilde       color235        default     # blank line padding

color   hdrdefault  color166        default     # default headers
color   header      color208        default     "^From:"
color   header      color208        default     "^Subject:"

color   quoted      color033        default     # quote colours
color   quoted1     color118        default
color   quoted2     color197        default
color   quoted3     color208        default
color   quoted4     color226        default
color   quoted5     color135        default

color   body        color070        default     "^gpg: Good signature from"
color   body        color196        default     "^gpg: Bad signature from"
color   body        color196        default     "^gpg: BAD signature from"
color   body        color196        default     "^gpg: Note: This key has expired!"
color   body        color226        default     "^gpg: WARNING: This key is not certified with a trusted signature!"
color   body        color226        default     "^gpg:          There is no indication that the signature belongs to the owner."
color   body        color226        default     "^gpg: can't handle these multiple signatures"
color   body        color226        default     "^gpg: signature verification suppressed"
color   body        color226        default     "^gpg: invalid node with packet of type"

# vi: set ft=muttrc :
